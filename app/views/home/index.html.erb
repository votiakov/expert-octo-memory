<h1>Listing Items</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= item.name %></td>
    <td><%= number_to_currency(item.price, :unit => "£") %></td>
  </tr>
<% end %>
</table>

<br>